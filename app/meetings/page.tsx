import { ModulePlaceholder } from "@/components/ans/module-placeholder";

export const metadata = { title: "Meetings & Travel" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Meetings & Travel"
      description="Client meetings, travel plans and the contact history behind them."
    />
  );
}
