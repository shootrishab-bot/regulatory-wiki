import { ModulePlaceholder } from "@/components/ans/module-placeholder";

export const metadata = { title: "Credentials" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Credentials"
      description="The firm's deal and matter credentials, searchable by sector, value and deal role."
    />
  );
}
