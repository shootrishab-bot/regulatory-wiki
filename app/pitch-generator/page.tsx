import { ModulePlaceholder } from "@/components/ans/module-placeholder";

export const metadata = { title: "Pitch Generator" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Pitch Generator"
      description="Assembles a pitch from credentials, partner profiles and practice profiles against a target brief."
    />
  );
}
